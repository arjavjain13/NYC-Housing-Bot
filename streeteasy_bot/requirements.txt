requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
PyYAML==6.0.2
python-dotenv==1.0.1
# Browser backend is now optional (HTTP mode is the default). Only needed if you
# set fetch_mode: browser in config.yaml.
playwright==1.49.1
playwright-stealth==1.0.6
